---
# SUSE (SLE 15) prerequisites for Distribution — the zypper analogue of tasks/RedHat.yml.
- name: Install prerequisite packages
  become: true
  ansible.builtin.package:
    name:
      - expect
    state: present
